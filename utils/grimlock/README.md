# Grimlock
Grimlock is a simple password manager.

## How to use
Open the terminal and launch the grimlock.bat script **[located in ../run_scripts]**, or use Python to run the `grimlock.py` file.
Without any arguments, the script launches in visual mode. To access CLI mode, run the `-help` command for more information.

## dependencies
- pyperclip
- cryptography